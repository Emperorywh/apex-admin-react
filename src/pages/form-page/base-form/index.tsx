import { IProps } from "./index.types";

const Index = (props: IProps) => {

    const { } = props;

    return (
        <div>
            <div>基础表单</div>
        </div>
    )
}

export default Index;
