import { IProps } from "./index.types";

const Index = (props: IProps) => {

    const { } = props;

    return (
        <div>
            <div>高级表单</div>
        </div>
    )
}

export default Index;
