import { IProps } from "./index.types";

const Index = (props: IProps) => {

    const { } = props;

    return (
        <div>
            <div>数据大屏</div>
        </div>
    )
}

export default Index;
