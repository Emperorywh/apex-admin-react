import { IProps } from "./index.types";

const Index = (props: IProps) => {

    const { } = props;

    return (
        <div>
            <div>edit-table</div>
        </div>
    )
}

export default Index;
