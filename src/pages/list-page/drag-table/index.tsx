import { IProps } from "./index.types";

const Index = (props: IProps) => {

    const { } = props;

    return (
        <div>
            <div>drag-table</div>
        </div>
    )
}

export default Index;
