import { IProps } from "./index.types";

const Index = (props: IProps) => {

    const { } = props;

    return (
        <div>
            <div>pro-table</div>
        </div>
    )
}

export default Index;
